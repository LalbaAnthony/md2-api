name: unit

on:
  workflow_call:
    inputs:
      node-version:
        description: Node.js version used to run the unit suite.
        required: false
        type: string
        default: "22"

jobs:
  unit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ inputs.node-version }}
          cache: npm
      - run: npm ci
      - run: npx vitest run tests/unit tests/contract --coverage
