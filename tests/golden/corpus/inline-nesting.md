Plain text, **bold**, _italic_, _**bold italic**_, ~~struck~~ and `code`.

A **bold run with _nested italic_ inside** it.

An _italic run with `inline code` inside_ it.

A ~~struck run with **bold** inside~~ it.

Hard break here:
line two after a hard break.
